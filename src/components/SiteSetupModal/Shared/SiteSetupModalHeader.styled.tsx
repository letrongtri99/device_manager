import styled from '@emotion/styled';
import { Typography } from '@material-ui/core';

export const SiteSetupModalHeaderSubtitle = styled(Typography)`
  font-weight: 400;
  color: ${({ theme }) => theme.palette.grey[500]};
`;
