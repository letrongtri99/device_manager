import styled from '@emotion/styled';
import { Box } from '@material-ui/core';

export const DashboardBackground = styled(Box)`
  background-color: ${({ theme }) => theme.palette.background.default};
`;
